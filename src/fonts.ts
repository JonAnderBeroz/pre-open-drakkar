import {Inter, Rock_Salt, Teko} from "next/font/google";

export const teko = Teko({subsets: ["latin"]});
export const inter = Inter({subsets: ["latin"]});
export const rockSalt = Rock_Salt({weight: "400", subsets: ["latin"]});
